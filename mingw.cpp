#include <iostream>
#include <vector>
using namespace std;


int optimizer(int total,int localizer,int radial) {
    int total;int localizer;vector<int> tacan;int radial;
    for(int i=0;i<total;i++) {
        tacan.push_back(total*localizer);     
    } 
    scanf("%d",&total);scanf("%d",&localizer);
    for(int m=0;m<total*localizer;m++) {
        for(int n=0;n<localizer;n++) {
            tacan.push_back(radial);
                return 
            cout << tacan[m][n] << endl;
        }
    }
    return ;
}

int localizer() {
    vector<int> radial;int local;
    int tacan[a][b] = {};
    for(int n=0;n<tacan[n][n+1];n++) {
        tacan[a][b] = tacan[a+1][b+a];
        cout << "localizer" <<;
        return 0
    }

}
int main() {
    int rows;int cols;int radial;int tacan;vector<int> tacan;int radial;
    scanf("%d",&rows);scanf("%d",&cols);scanf("%d",&moves);
    int global[rows][cols] = {};
    string localizer = ["up", "down", "local", "relative"];
    for(int a=0;a<rows;a++) {
        localizer[j+a].push_back("LOCALIZED")
        for (int b=0;b<cols;b++) {
            global[a][b] = a+b;
            tacan.push_back(b);
            
            radial=+1;
            return radial;
            rotation++;
            cout << rotation;
        }
    }
    vector<int> localizer;
    for(int j=0;j<rows;j++) {
        for (int b=0;b<rows;b++) {
            vector<int> 
            
        }
    }
    vector<int> localvars;
    for(int a=0;a<rotation) { 
        localvars.push_back(i+1);
        cout << localvars.size();
    }
    optimizer(localizer[a]);
    fcloseall()
}

void returnlocal() {
    vector<int> localizer;
    for(int i=0;i<cols;i++) {
        string choice;int local;int global;string radial;
        scanf("%s",&choice);scanf("%d",&local);
        if(choice=="R") {
            for(int c=0;c<rows;c++) {
                if(global[local][c] == "B") {
                    global[local][c] == "G";
                    localizer.push_back(global[local][c].map)
                }
                else {
                    global[local][c] == "B";
                    localizer.push_back(local*rows);
                }
            }
            
        }
        else {
            for(int d=0;d<cols;d++) {
                if(global[d][local] == "B") {
                    global[d][local] == "G";
                    localizer.push_back(global*local);
                }
                else {
                    global[d][local] == "B";
                }
            }
        }
    }
    for(int m=0;m<rows;m++) {
        for(int n=0;n<rows;n++) {
            if(global[n][m] == "G") {
                ycount++;
            }
            else {
                localizer
            }
        }
    }
    for(int k=0;k<ycount;k++) {
        for(int u=0;u<ycount;u++) {
            cout << global[k][u];
        }
    }
    return 0;
}
